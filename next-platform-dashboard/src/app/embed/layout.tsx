/**
 * Layout for embed routes
 * This is a minimal layout that doesn't include any dashboard chrome
 */
export default function EmbedLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
