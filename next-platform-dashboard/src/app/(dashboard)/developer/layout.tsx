// src/app/(dashboard)/developer/layout.tsx
// Phase EM-43: Developer Portal Layout

import { ReactNode } from "react";

export default function DeveloperLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
}
